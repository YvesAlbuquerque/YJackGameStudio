# AGENTS.local.md Template

Copy this file to the project root as `AGENTS.local.md` for private local notes
that should not be committed. Keep durable team rules in `AGENTS.md` or `.agents/`.

## Local Environment

- OS:
- Shell:
- Engine install path:
- Preferred editor:

## Local Commands

Document commands that only work on this machine, such as engine executable paths
or local asset pipeline shortcuts.

## Personal Workflow Notes

Keep this section limited to personal preferences. Do not duplicate project rules
that belong in tracked files.

## Setup

1. Copy this template to `AGENTS.local.md` at the repository root.
2. Add only machine-specific notes.
3. Verify `AGENTS.local.md` is ignored by git.
