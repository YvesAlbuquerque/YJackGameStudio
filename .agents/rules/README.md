# Agent Rules

These files are workspace-level rule supplements for agentic IDEs that can read
Markdown rules from `.agents/rules/` or when an agent is explicitly directed to
read this directory.

Root `AGENTS.md` remains the source of truth. Use these files for path/domain
specific constraints only.
