# Antigravity Compatibility Rule

Canonical project instructions live in `../../AGENTS.md`.
Canonical shared rules live in `../../.agents/rules/`.

Do not duplicate or edit rule content here. Read the root `AGENTS.md`, then read
`.agents/rules/README.md` and the path-specific rule file that matches the files
you are editing.
